export const mergeTranslations = (base: any, overrides: any) => ({ ...base, ...overrides });

export type ModelOptionProvider = any;
export type ModelOptionsResult = any;

export const applyDocumentLocale = (locale: string) => {};
export const LOCALE_ENDONYMS: Record<string, string> = { en: "English" };

export const parseColor = (color: string) => color;
export const THEME_PRESET_PALETTES = {};
